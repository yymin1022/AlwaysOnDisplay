apply plugin: 'com.android.application'

android {
    signingConfigs {
        Default {
            keyAlias 'yymin1022'
            keyPassword 'dydals1022'
            storeFile file('D:/AppProjects/debug.keystore')
            storePassword 'dydals1022'
        }
    }
    compileSdkVersion 28
    defaultConfig {
        applicationId "com.yong.aod"
        minSdkVersion 16
        targetSdkVersion 28
        versionCode 1
        versionName "1.0"
        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }

        buildTypes.each {
            it.buildConfigField 'String', "CaulyID", AlwaysOnDisplay_CaulyID
            it.buildConfigField 'String', "MerchantID", AlwaysOnDIsplay_MerchantID
            it.buildConfigField 'String', "LicenseKey", AlwaysOnDisplay_LicenseKey
        }
    }
}

dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
    implementation 'androidx.appcompat:appcompat:1.1.0-alpha01'
    implementation 'androidx.constraintlayout:constraintlayout:1.1.3'
    implementation 'com.anjlab.android.iab.v3:library:1.0.44'
    implementation 'com.ssomai:android.scalablelayout:2.1.6'
    implementation 'gun0912.ted:tedpermission:2.2.2'
    testImplementation 'junit:junit:4.12'
    androidTestImplementation 'androidx.test:runner:1.1.1'
    androidTestImplementation 'androidx.test.espresso:espresso-core:3.1.1'
    implementation 'org.jetbrains:annotations-java5:15.0'
}
